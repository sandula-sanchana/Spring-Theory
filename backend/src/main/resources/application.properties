spring.application.name=backend

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/possys?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Asia/Colombo
spring.datasource.username=root
spring.datasource.password=Sandula#2026$MySQL!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ? REMOVE this (Hibernate 7 doesn't have it)
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ? Optional (correct spelling, but not required)
# spring.jpa.generate-ddl=true
